import React from 'react';
import PropTypes from 'prop-types';
import styles from './styles.module.scss';

function Body(props) {
  return (
    <div className={styles.Body}>
      BSC tools provides you with multiple tools to give you an edge over other
      traders
    </div>
  );
}

Body.propTypes = {};

export default Body;
